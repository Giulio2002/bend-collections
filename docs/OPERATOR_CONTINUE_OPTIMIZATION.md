# Continue the complete objective

Known slow rows are unfinished work, not a reason to checkpoint as complete. After collecting the current gate, continue optimizing all remaining operations and keep the actual implementation proofs current. The user already authorized indexed/native-array representations and the closest efficient equivalent of optimized C.

No new user permission is needed to implement the indexed-child trie and an independently reviewable corresponding C prototype in NEW experimental paths. Preserve every protected C/reference/workload file and the old measurements; do not replace a protected reference until equivalence and optimized-C quality have been reviewed. This prototype work is authorized now; do not block all progress on a benchmark-table decision. Any eventual reference update must preserve workload semantics and strengthen the comparison rather than slow C. Compare both references where useful.

List construction overhead is a reason to improve the real public API with packed-array/view outputs (and their proofs) where lists are unnecessary, not a reason to exempt rows, hide conversion cost or weaken the 2.5x requirement. Preserve agreed behavior and provide a fair end-to-end comparison.

Capacity-restricted LRU trace laws and U32/String instantiations are useful checked progress, but do not claim the entire accepted API range is proven. Final source provenance must cover all checks.
