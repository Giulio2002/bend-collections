# GENERATED by tools/generators/trace_data.py (bitset); do not edit.
# Regenerate: python3 tools/generators/trace_data.py bitset
import Base
import ../lib/logic.bend as L
import ../lib/list.bend as LL
import ../../src/bitset.bend as B
import ../../spec/bitset.bend as S
import ../../types/bitset.bend as E
import ./state.bend as ST
import ./steps.bend as BS

# Arbitrary finite operation traces: the real runner refines the spec runner.

def view(r: B.Bitset & List<&2, E.Obs>) -> List<&2, Bool> & List<&2, E.Obs>:
  match r:
    case Tuple{d, os}:
      (ST.abs(d), os)

def srun_obs(ops: List<&2, E.Op>, m: List<&2, Bool>) -> List<&2, E.Obs>:
  Pair.snd(List<&2, Bool>, List<&2, E.Obs>, S.run(ops, m))

def srun_state(ops: List<&2, E.Op>, m: List<&2, Bool>) -> List<&2, Bool>:
  Pair.fst(List<&2, Bool>, List<&2, E.Obs>, S.run(ops, m))

def next(d: B.Bitset, op: E.Op) -> B.Bitset:
  Pair.fst(B.Bitset, E.Obs, B.step(d, op))

def obs(d: B.Bitset, op: E.Op) -> E.Obs:
  Pair.snd(B.Bitset, E.Obs, B.step(d, op))

def run_cons(+op: E.Op, +rest: List<&2, E.Op>, +d: B.Bitset, +acc: List<&2, E.Obs>) -> {B.run_acc(Con{op, rest}, (d, acc)) == B.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : B.Bitset & List<&2, E.Obs>}:
  %Equal.sym(B.Bitset & E.Obs, B.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(B.Bitset, E.Obs, B.step(d, op))) : {B.run_acc(rest, B.record(acc, _)) == B.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : B.Bitset & List<&2, E.Obs>}
  {==}

def spec_view(+d: B.Bitset, +op: E.Op, +g: {ST.inv(d) == True{} : Bool}) -> {(ST.abs(next(d, op)), obs(d, op)) == S.step(ST.abs(d), op) : List<&2, Bool> & E.Obs}:
  %Pair.fst({BS.view1(B.step(d, op)) == S.step(ST.abs(d), op) : List<&2, Bool> & E.Obs}, {ST.inv(next(d, op)) == True{} : Bool}, BS.step_ok(d, op, g)) : {(ST.abs(next(d, op)), obs(d, op)) == _ : List<&2, Bool> & E.Obs}
  %Equal.sym(B.Bitset & E.Obs, B.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(B.Bitset, E.Obs, B.step(d, op))) : {(ST.abs(next(d, op)), obs(d, op)) == BS.view1(_) : List<&2, Bool> & E.Obs}
  {==}

def spec_run_cons(+op: E.Op, +rest: List<&2, E.Op>, +m: List<&2, Bool>, +m1: List<&2, Bool>, +ob: E.Obs, +e: {(m1, ob) == S.step(m, op) : List<&2, Bool> & E.Obs}) -> {S.run(Con{op, rest}, m) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Bool> & List<&2, E.Obs>}:
  %e : {S.cons_obs(Pair.snd(List<&2, Bool>, E.Obs, _), S.run(rest, Pair.fst(List<&2, Bool>, E.Obs, _))) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Bool> & List<&2, E.Obs>}
  %Equal.sym(List<&2, Bool> & List<&2, E.Obs>, S.run(rest, m1), (srun_state(rest, m1), srun_obs(rest, m1)), L.pair_eta(List<&2, Bool>, List<&2, E.Obs>, S.run(rest, m1))) : {S.cons_obs(ob, _) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Bool> & List<&2, E.Obs>}
  {==}

def fin(ops: List<&2, E.Op>, d: B.Bitset, acc: List<&2, E.Obs>) -> B.Bitset:
  Pair.fst(B.Bitset, List<&2, E.Obs>, B.run_acc(ops, (d, acc)))

def RunOK(ops: List<&2, E.Op>, d: B.Bitset, acc: List<&2, E.Obs>) -> Type:
  {B.run_acc(ops, (d, acc)) == (fin(ops, d, acc), List.reverse.go(&2, E.Obs, srun_obs(ops, ST.abs(d)), acc)) : B.Bitset & List<&2, E.Obs>} & ({ST.abs(fin(ops, d, acc)) == srun_state(ops, ST.abs(d)) : List<&2, Bool>} & {ST.inv(fin(ops, d, acc)) == True{} : Bool})

def run_ok(+ops: List<&2, E.Op>, +d: B.Bitset, +acc: List<&2, E.Obs>, +g: {ST.inv(d) == True{} : Bool}) -> RunOK(ops, d, acc):
  match ops:
    case Nil{}:
      ({==}, ({==}, g))
    case Con{+op, +rest}:
      +d1 = next(d, op)
      +ob = obs(d, op)
      +acc1 = {Con{ob, acc} : List<&2, E.Obs>}
      +g1 = Pair.snd({BS.view1(B.step(d, op)) == S.step(ST.abs(d), op) : List<&2, Bool> & E.Obs}, {ST.inv(d1) == True{} : Bool}, BS.step_ok(d, op, g))
      +es = spec_run_cons(op, rest, ST.abs(d), ST.abs(d1), ob, spec_view(d, op, g))
      +rc = run_cons(op, rest, d, acc)
      -P1 = {B.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, srun_obs(rest, ST.abs(d1)), acc1)) : B.Bitset & List<&2, E.Obs>}
      -P2 = {ST.abs(fin(rest, d1, acc1)) == srun_state(rest, ST.abs(d1)) : List<&2, Bool>}
      -P3 = {ST.inv(fin(rest, d1, acc1)) == True{} : Bool}
      -I23 = P2 & P3
      (%Equal.sym(B.Bitset & List<&2, E.Obs>, B.run_acc(Con{op, rest}, (d, acc)), B.run_acc(rest, (d1, acc1)), rc) : {_ == (Pair.fst(B.Bitset, List<&2, E.Obs>, _), List.reverse.go(&2, E.Obs, srun_obs(Con{op, rest}, ST.abs(d)), acc)) : B.Bitset & List<&2, E.Obs>}
       %Equal.sym(List<&2, Bool> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {B.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, Pair.snd(List<&2, Bool>, List<&2, E.Obs>, _), acc)) : B.Bitset & List<&2, E.Obs>}
       Pair.fst(P1, I23, run_ok(rest, d1, acc1, g1)),
       (%Equal.sym(B.Bitset & List<&2, E.Obs>, B.run_acc(Con{op, rest}, (d, acc)), B.run_acc(rest, (d1, acc1)), rc) : {ST.abs(Pair.fst(B.Bitset, List<&2, E.Obs>, _)) == srun_state(Con{op, rest}, ST.abs(d)) : List<&2, Bool>}
        %Equal.sym(List<&2, Bool> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {ST.abs(fin(rest, d1, acc1)) == Pair.fst(List<&2, Bool>, List<&2, E.Obs>, _) : List<&2, Bool>}
        Pair.fst(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1))),
        %Equal.sym(B.Bitset & List<&2, E.Obs>, B.run_acc(Con{op, rest}, (d, acc)), B.run_acc(rest, (d1, acc1)), rc) : {ST.inv(Pair.fst(B.Bitset, List<&2, E.Obs>, _)) == True{} : Bool}
        Pair.snd(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1)))))

# From any state satisfying the invariant, src.run refines spec.run.
def trace_from(+ops: List<&2, E.Op>, +d0: B.Bitset, +g0: {ST.inv(d0) == True{} : Bool}) -> {view(B.run(ops, d0)) == S.run(ops, ST.abs(d0)) : List<&2, Bool> & List<&2, E.Obs>}:
  +m0 = ST.abs(d0)
  -P1 = {B.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : B.Bitset & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, Bool>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(B.Bitset & List<&2, E.Obs>, B.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {view(B.finish(_)) == S.run(ops, m0) : List<&2, Bool> & List<&2, E.Obs>}
  %Equal.sym(List<&2, E.Obs>, List.reverse(&2, E.Obs, List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), srun_obs(ops, m0), LL.rev_rev(E.Obs, srun_obs(ops, m0))) : {(ST.abs(fin(ops, d0, Nil{})), _) == S.run(ops, m0) : List<&2, Bool> & List<&2, E.Obs>}
  %Equal.sym(List<&2, Bool>, ST.abs(fin(ops, d0, Nil{})), srun_state(ops, m0), Pair.fst(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))) : {(_, srun_obs(ops, m0)) == S.run(ops, m0) : List<&2, Bool> & List<&2, E.Obs>}
  Equal.sym(List<&2, Bool> & List<&2, E.Obs>, S.run(ops, m0), (srun_state(ops, m0), srun_obs(ops, m0)), L.pair_eta(List<&2, Bool>, List<&2, E.Obs>, S.run(ops, m0)))

def inv_from(+ops: List<&2, E.Op>, +d0: B.Bitset, +g0: {ST.inv(d0) == True{} : Bool}) -> {ST.inv(Pair.fst(B.Bitset, List<&2, E.Obs>, B.run(ops, d0))) == True{} : Bool}:
  +m0 = ST.abs(d0)
  -P1 = {B.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : B.Bitset & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, Bool>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(B.Bitset & List<&2, E.Obs>, B.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {ST.inv(Pair.fst(B.Bitset, List<&2, E.Obs>, B.finish(_))) == True{} : Bool}
  Pair.snd(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))
