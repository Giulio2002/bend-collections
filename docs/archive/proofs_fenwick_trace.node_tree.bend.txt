# GENERATED by tools/generators/trace_data.py (fenwick_tree); do not edit.
# Regenerate: python3 tools/generators/trace_data.py fenwick_tree
import Base
import ../lib/logic.bend as L
import ../lib/list.bend as LL
import ../../src/fenwick_tree.bend as F
import ../../spec/fenwick_tree.bend as S
import ../../types/fenwick_tree.bend as E
import ./steps.bend as ST

# Arbitrary finite operation traces: the real runner refines the spec runner.

def view(r: F.Fenwick & List<&2, E.Obs>) -> List<&2, U32> & List<&2, E.Obs>:
  match r:
    case Tuple{d, os}:
      (ST.abs(d), os)

def srun_obs(ops: List<&2, E.Op>, m: List<&2, U32>) -> List<&2, E.Obs>:
  Pair.snd(List<&2, U32>, List<&2, E.Obs>, S.run(ops, m))

def srun_state(ops: List<&2, E.Op>, m: List<&2, U32>) -> List<&2, U32>:
  Pair.fst(List<&2, U32>, List<&2, E.Obs>, S.run(ops, m))

def next(d: F.Fenwick, op: E.Op) -> F.Fenwick:
  Pair.fst(F.Fenwick, E.Obs, F.step(d, op))

def obs(d: F.Fenwick, op: E.Op) -> E.Obs:
  Pair.snd(F.Fenwick, E.Obs, F.step(d, op))

def run_cons(+op: E.Op, +rest: List<&2, E.Op>, +d: F.Fenwick, +acc: List<&2, E.Obs>) -> {F.run_acc(Con{op, rest}, (d, acc)) == F.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : F.Fenwick & List<&2, E.Obs>}:
  %Equal.sym(F.Fenwick & E.Obs, F.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(F.Fenwick, E.Obs, F.step(d, op))) : {F.run_acc(rest, F.record(acc, _)) == F.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : F.Fenwick & List<&2, E.Obs>}
  {==}

def spec_view(+d: F.Fenwick, +op: E.Op, +g: {ST.inv(d) == True{} : Bool}) -> {(ST.abs(next(d, op)), obs(d, op)) == S.step(ST.abs(d), op) : List<&2, U32> & E.Obs}:
  %Pair.fst({ST.view1(F.step(d, op)) == S.step(ST.abs(d), op) : List<&2, U32> & E.Obs}, {ST.inv(next(d, op)) == True{} : Bool}, ST.step_ok(d, op, g)) : {(ST.abs(next(d, op)), obs(d, op)) == _ : List<&2, U32> & E.Obs}
  %Equal.sym(F.Fenwick & E.Obs, F.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(F.Fenwick, E.Obs, F.step(d, op))) : {(ST.abs(next(d, op)), obs(d, op)) == ST.view1(_) : List<&2, U32> & E.Obs}
  {==}

def spec_run_cons(+op: E.Op, +rest: List<&2, E.Op>, +m: List<&2, U32>, +m1: List<&2, U32>, +ob: E.Obs, +e: {(m1, ob) == S.step(m, op) : List<&2, U32> & E.Obs}) -> {S.run(Con{op, rest}, m) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, U32> & List<&2, E.Obs>}:
  %e : {S.cons_obs(Pair.snd(List<&2, U32>, E.Obs, _), S.run(rest, Pair.fst(List<&2, U32>, E.Obs, _))) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, U32> & List<&2, E.Obs>}
  %Equal.sym(List<&2, U32> & List<&2, E.Obs>, S.run(rest, m1), (srun_state(rest, m1), srun_obs(rest, m1)), L.pair_eta(List<&2, U32>, List<&2, E.Obs>, S.run(rest, m1))) : {S.cons_obs(ob, _) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, U32> & List<&2, E.Obs>}
  {==}

def fin(ops: List<&2, E.Op>, d: F.Fenwick, acc: List<&2, E.Obs>) -> F.Fenwick:
  Pair.fst(F.Fenwick, List<&2, E.Obs>, F.run_acc(ops, (d, acc)))

def RunOK(ops: List<&2, E.Op>, d: F.Fenwick, acc: List<&2, E.Obs>) -> Type:
  {F.run_acc(ops, (d, acc)) == (fin(ops, d, acc), List.reverse.go(&2, E.Obs, srun_obs(ops, ST.abs(d)), acc)) : F.Fenwick & List<&2, E.Obs>} & ({ST.abs(fin(ops, d, acc)) == srun_state(ops, ST.abs(d)) : List<&2, U32>} & {ST.inv(fin(ops, d, acc)) == True{} : Bool})

def run_ok(+ops: List<&2, E.Op>, +d: F.Fenwick, +acc: List<&2, E.Obs>, +g: {ST.inv(d) == True{} : Bool}) -> RunOK(ops, d, acc):
  match ops:
    case Nil{}:
      ({==}, ({==}, g))
    case Con{+op, +rest}:
      +d1 = next(d, op)
      +ob = obs(d, op)
      +acc1 = {Con{ob, acc} : List<&2, E.Obs>}
      +g1 = Pair.snd({ST.view1(F.step(d, op)) == S.step(ST.abs(d), op) : List<&2, U32> & E.Obs}, {ST.inv(d1) == True{} : Bool}, ST.step_ok(d, op, g))
      +es = spec_run_cons(op, rest, ST.abs(d), ST.abs(d1), ob, spec_view(d, op, g))
      +rc = run_cons(op, rest, d, acc)
      -P1 = {F.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, srun_obs(rest, ST.abs(d1)), acc1)) : F.Fenwick & List<&2, E.Obs>}
      -P2 = {ST.abs(fin(rest, d1, acc1)) == srun_state(rest, ST.abs(d1)) : List<&2, U32>}
      -P3 = {ST.inv(fin(rest, d1, acc1)) == True{} : Bool}
      -I23 = P2 & P3
      (%Equal.sym(F.Fenwick & List<&2, E.Obs>, F.run_acc(Con{op, rest}, (d, acc)), F.run_acc(rest, (d1, acc1)), rc) : {_ == (Pair.fst(F.Fenwick, List<&2, E.Obs>, _), List.reverse.go(&2, E.Obs, srun_obs(Con{op, rest}, ST.abs(d)), acc)) : F.Fenwick & List<&2, E.Obs>}
       %Equal.sym(List<&2, U32> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {F.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, Pair.snd(List<&2, U32>, List<&2, E.Obs>, _), acc)) : F.Fenwick & List<&2, E.Obs>}
       Pair.fst(P1, I23, run_ok(rest, d1, acc1, g1)),
       (%Equal.sym(F.Fenwick & List<&2, E.Obs>, F.run_acc(Con{op, rest}, (d, acc)), F.run_acc(rest, (d1, acc1)), rc) : {ST.abs(Pair.fst(F.Fenwick, List<&2, E.Obs>, _)) == srun_state(Con{op, rest}, ST.abs(d)) : List<&2, U32>}
        %Equal.sym(List<&2, U32> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {ST.abs(fin(rest, d1, acc1)) == Pair.fst(List<&2, U32>, List<&2, E.Obs>, _) : List<&2, U32>}
        Pair.fst(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1))),
        %Equal.sym(F.Fenwick & List<&2, E.Obs>, F.run_acc(Con{op, rest}, (d, acc)), F.run_acc(rest, (d1, acc1)), rc) : {ST.inv(Pair.fst(F.Fenwick, List<&2, E.Obs>, _)) == True{} : Bool}
        Pair.snd(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1)))))

# From any state satisfying the invariant, src.run refines spec.run.
def trace_from(+ops: List<&2, E.Op>, +d0: F.Fenwick, +g0: {ST.inv(d0) == True{} : Bool}) -> {view(F.run(ops, d0)) == S.run(ops, ST.abs(d0)) : List<&2, U32> & List<&2, E.Obs>}:
  +m0 = ST.abs(d0)
  -P1 = {F.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : F.Fenwick & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, U32>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(F.Fenwick & List<&2, E.Obs>, F.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {view(F.finish(_)) == S.run(ops, m0) : List<&2, U32> & List<&2, E.Obs>}
  %Equal.sym(List<&2, E.Obs>, List.reverse(&2, E.Obs, List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), srun_obs(ops, m0), LL.rev_rev(E.Obs, srun_obs(ops, m0))) : {(ST.abs(fin(ops, d0, Nil{})), _) == S.run(ops, m0) : List<&2, U32> & List<&2, E.Obs>}
  %Equal.sym(List<&2, U32>, ST.abs(fin(ops, d0, Nil{})), srun_state(ops, m0), Pair.fst(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))) : {(_, srun_obs(ops, m0)) == S.run(ops, m0) : List<&2, U32> & List<&2, E.Obs>}
  Equal.sym(List<&2, U32> & List<&2, E.Obs>, S.run(ops, m0), (srun_state(ops, m0), srun_obs(ops, m0)), L.pair_eta(List<&2, U32>, List<&2, E.Obs>, S.run(ops, m0)))

def inv_from(+ops: List<&2, E.Op>, +d0: F.Fenwick, +g0: {ST.inv(d0) == True{} : Bool}) -> {ST.inv(Pair.fst(F.Fenwick, List<&2, E.Obs>, F.run(ops, d0))) == True{} : Bool}:
  +m0 = ST.abs(d0)
  -P1 = {F.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : F.Fenwick & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, U32>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(F.Fenwick & List<&2, E.Obs>, F.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {ST.inv(Pair.fst(F.Fenwick, List<&2, E.Obs>, F.finish(_))) == True{} : Bool}
  Pair.snd(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))
