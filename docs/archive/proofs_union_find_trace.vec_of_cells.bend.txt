# GENERATED by tools/generators/trace_data.py (union_find); do not edit.
# Regenerate: python3 tools/generators/trace_data.py union_find
import Base
import ../lib/logic.bend as L
import ../lib/list.bend as LL
import ../../src/union_find.bend as U
import ../../spec/union_find.bend as S
import ../../types/union_find.bend as E
import ./steps.bend as ST

# Arbitrary finite operation traces: the real runner refines the spec runner.

def view(r: U.UnionFind & List<&2, E.Obs>) -> List<&2, Nat> & List<&2, E.Obs>:
  match r:
    case Tuple{d, os}:
      (ST.abs(d), os)

def srun_obs(ops: List<&2, E.Op>, m: List<&2, Nat>) -> List<&2, E.Obs>:
  Pair.snd(List<&2, Nat>, List<&2, E.Obs>, S.run(ops, m))

def srun_state(ops: List<&2, E.Op>, m: List<&2, Nat>) -> List<&2, Nat>:
  Pair.fst(List<&2, Nat>, List<&2, E.Obs>, S.run(ops, m))

def next(d: U.UnionFind, op: E.Op) -> U.UnionFind:
  Pair.fst(U.UnionFind, E.Obs, U.step(d, op))

def obs(d: U.UnionFind, op: E.Op) -> E.Obs:
  Pair.snd(U.UnionFind, E.Obs, U.step(d, op))

def run_cons(+op: E.Op, +rest: List<&2, E.Op>, +d: U.UnionFind, +acc: List<&2, E.Obs>) -> {U.run_acc(Con{op, rest}, (d, acc)) == U.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : U.UnionFind & List<&2, E.Obs>}:
  %Equal.sym(U.UnionFind & E.Obs, U.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(U.UnionFind, E.Obs, U.step(d, op))) : {U.run_acc(rest, U.record(acc, _)) == U.run_acc(rest, (next(d, op), Con{obs(d, op), acc})) : U.UnionFind & List<&2, E.Obs>}
  {==}

def spec_view(+d: U.UnionFind, +op: E.Op, +g: {ST.inv(d) == True{} : Bool}) -> {(ST.abs(next(d, op)), obs(d, op)) == S.step(ST.abs(d), op) : List<&2, Nat> & E.Obs}:
  %Pair.fst({ST.view1(U.step(d, op)) == S.step(ST.abs(d), op) : List<&2, Nat> & E.Obs}, {ST.inv(next(d, op)) == True{} : Bool}, ST.step_ok(d, op, g)) : {(ST.abs(next(d, op)), obs(d, op)) == _ : List<&2, Nat> & E.Obs}
  %Equal.sym(U.UnionFind & E.Obs, U.step(d, op), (next(d, op), obs(d, op)), L.pair_eta(U.UnionFind, E.Obs, U.step(d, op))) : {(ST.abs(next(d, op)), obs(d, op)) == ST.view1(_) : List<&2, Nat> & E.Obs}
  {==}

def spec_run_cons(+op: E.Op, +rest: List<&2, E.Op>, +m: List<&2, Nat>, +m1: List<&2, Nat>, +ob: E.Obs, +e: {(m1, ob) == S.step(m, op) : List<&2, Nat> & E.Obs}) -> {S.run(Con{op, rest}, m) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Nat> & List<&2, E.Obs>}:
  %e : {S.cons_obs(Pair.snd(List<&2, Nat>, E.Obs, _), S.run(rest, Pair.fst(List<&2, Nat>, E.Obs, _))) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Nat> & List<&2, E.Obs>}
  %Equal.sym(List<&2, Nat> & List<&2, E.Obs>, S.run(rest, m1), (srun_state(rest, m1), srun_obs(rest, m1)), L.pair_eta(List<&2, Nat>, List<&2, E.Obs>, S.run(rest, m1))) : {S.cons_obs(ob, _) == (srun_state(rest, m1), Con{ob, srun_obs(rest, m1)}) : List<&2, Nat> & List<&2, E.Obs>}
  {==}

def fin(ops: List<&2, E.Op>, d: U.UnionFind, acc: List<&2, E.Obs>) -> U.UnionFind:
  Pair.fst(U.UnionFind, List<&2, E.Obs>, U.run_acc(ops, (d, acc)))

def RunOK(ops: List<&2, E.Op>, d: U.UnionFind, acc: List<&2, E.Obs>) -> Type:
  {U.run_acc(ops, (d, acc)) == (fin(ops, d, acc), List.reverse.go(&2, E.Obs, srun_obs(ops, ST.abs(d)), acc)) : U.UnionFind & List<&2, E.Obs>} & ({ST.abs(fin(ops, d, acc)) == srun_state(ops, ST.abs(d)) : List<&2, Nat>} & {ST.inv(fin(ops, d, acc)) == True{} : Bool})

def run_ok(+ops: List<&2, E.Op>, +d: U.UnionFind, +acc: List<&2, E.Obs>, +g: {ST.inv(d) == True{} : Bool}) -> RunOK(ops, d, acc):
  match ops:
    case Nil{}:
      ({==}, ({==}, g))
    case Con{+op, +rest}:
      +d1 = next(d, op)
      +ob = obs(d, op)
      +acc1 = {Con{ob, acc} : List<&2, E.Obs>}
      +g1 = Pair.snd({ST.view1(U.step(d, op)) == S.step(ST.abs(d), op) : List<&2, Nat> & E.Obs}, {ST.inv(d1) == True{} : Bool}, ST.step_ok(d, op, g))
      +es = spec_run_cons(op, rest, ST.abs(d), ST.abs(d1), ob, spec_view(d, op, g))
      +rc = run_cons(op, rest, d, acc)
      -P1 = {U.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, srun_obs(rest, ST.abs(d1)), acc1)) : U.UnionFind & List<&2, E.Obs>}
      -P2 = {ST.abs(fin(rest, d1, acc1)) == srun_state(rest, ST.abs(d1)) : List<&2, Nat>}
      -P3 = {ST.inv(fin(rest, d1, acc1)) == True{} : Bool}
      -I23 = P2 & P3
      (%Equal.sym(U.UnionFind & List<&2, E.Obs>, U.run_acc(Con{op, rest}, (d, acc)), U.run_acc(rest, (d1, acc1)), rc) : {_ == (Pair.fst(U.UnionFind, List<&2, E.Obs>, _), List.reverse.go(&2, E.Obs, srun_obs(Con{op, rest}, ST.abs(d)), acc)) : U.UnionFind & List<&2, E.Obs>}
       %Equal.sym(List<&2, Nat> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {U.run_acc(rest, (d1, acc1)) == (fin(rest, d1, acc1), List.reverse.go(&2, E.Obs, Pair.snd(List<&2, Nat>, List<&2, E.Obs>, _), acc)) : U.UnionFind & List<&2, E.Obs>}
       Pair.fst(P1, I23, run_ok(rest, d1, acc1, g1)),
       (%Equal.sym(U.UnionFind & List<&2, E.Obs>, U.run_acc(Con{op, rest}, (d, acc)), U.run_acc(rest, (d1, acc1)), rc) : {ST.abs(Pair.fst(U.UnionFind, List<&2, E.Obs>, _)) == srun_state(Con{op, rest}, ST.abs(d)) : List<&2, Nat>}
        %Equal.sym(List<&2, Nat> & List<&2, E.Obs>, S.run(Con{op, rest}, ST.abs(d)), (srun_state(rest, ST.abs(d1)), Con{ob, srun_obs(rest, ST.abs(d1))}), es) : {ST.abs(fin(rest, d1, acc1)) == Pair.fst(List<&2, Nat>, List<&2, E.Obs>, _) : List<&2, Nat>}
        Pair.fst(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1))),
        %Equal.sym(U.UnionFind & List<&2, E.Obs>, U.run_acc(Con{op, rest}, (d, acc)), U.run_acc(rest, (d1, acc1)), rc) : {ST.inv(Pair.fst(U.UnionFind, List<&2, E.Obs>, _)) == True{} : Bool}
        Pair.snd(P2, P3, Pair.snd(P1, I23, run_ok(rest, d1, acc1, g1)))))

# From any state satisfying the invariant, src.run refines spec.run.
def trace_from(+ops: List<&2, E.Op>, +d0: U.UnionFind, +g0: {ST.inv(d0) == True{} : Bool}) -> {view(U.run(ops, d0)) == S.run(ops, ST.abs(d0)) : List<&2, Nat> & List<&2, E.Obs>}:
  +m0 = ST.abs(d0)
  -P1 = {U.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : U.UnionFind & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, Nat>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(U.UnionFind & List<&2, E.Obs>, U.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {view(U.finish(_)) == S.run(ops, m0) : List<&2, Nat> & List<&2, E.Obs>}
  %Equal.sym(List<&2, E.Obs>, List.reverse(&2, E.Obs, List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), srun_obs(ops, m0), LL.rev_rev(E.Obs, srun_obs(ops, m0))) : {(ST.abs(fin(ops, d0, Nil{})), _) == S.run(ops, m0) : List<&2, Nat> & List<&2, E.Obs>}
  %Equal.sym(List<&2, Nat>, ST.abs(fin(ops, d0, Nil{})), srun_state(ops, m0), Pair.fst(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))) : {(_, srun_obs(ops, m0)) == S.run(ops, m0) : List<&2, Nat> & List<&2, E.Obs>}
  Equal.sym(List<&2, Nat> & List<&2, E.Obs>, S.run(ops, m0), (srun_state(ops, m0), srun_obs(ops, m0)), L.pair_eta(List<&2, Nat>, List<&2, E.Obs>, S.run(ops, m0)))

def inv_from(+ops: List<&2, E.Op>, +d0: U.UnionFind, +g0: {ST.inv(d0) == True{} : Bool}) -> {ST.inv(Pair.fst(U.UnionFind, List<&2, E.Obs>, U.run(ops, d0))) == True{} : Bool}:
  +m0 = ST.abs(d0)
  -P1 = {U.run_acc(ops, (d0, Nil{})) == (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})) : U.UnionFind & List<&2, E.Obs>}
  -P2 = {ST.abs(fin(ops, d0, Nil{})) == srun_state(ops, m0) : List<&2, Nat>}
  -P3 = {ST.inv(fin(ops, d0, Nil{})) == True{} : Bool}
  %Equal.sym(U.UnionFind & List<&2, E.Obs>, U.run_acc(ops, (d0, Nil{})), (fin(ops, d0, Nil{}), List.reverse.go(&2, E.Obs, srun_obs(ops, m0), Nil{})), Pair.fst(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0))) : {ST.inv(Pair.fst(U.UnionFind, List<&2, E.Obs>, U.finish(_))) == True{} : Bool}
  Pair.snd(P2, P3, Pair.snd(P1, P2 & P3, run_ok(ops, d0, Nil{}, g0)))
